# Foundation — Tasks

1. Expand fixture data from 2 clips to 20-30 with all apparatus, 3+ rotations, 3 teams, varied scores (including null and 0), varied durations (8s–39s), mock teamLogo URLs — COMPLETE
2. Create usePlayoutState.js hook with mock playout state: isPlayoutActive, currentMode, currentClip, clipQueue, cameraStates, overrideState, preloadState, coordinator heartbeat — NOT STARTED
3. Create usePlayoutActions.js hook with all action dispatchers: skipClip, forceCamera, releaseOverride, pausePlayout, resumePlayout, stopPlayout, flagMoment, addToQueue, retryClip, retryAllFailed, fetchClips — NOT STARTED
4. Create usePlayoutSimulation.js hook with timer logic: clip progress, score reveal trigger, simulated preloading, auto-advance, moment replay simulation, fallback mode entry — NOT STARTED
