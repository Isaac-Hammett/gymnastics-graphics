# Rundown — Tasks

1. Create PlayoutRulesEditor.jsx — clip order dropdown, apparatus priority, transition type selector (cut/crossfade/stinger disabled), crossfade duration slider, gap fill sequence list with add/remove/reorder/background/loop toggle — COMPLETE
2. Create ContentSequenceEditor.jsx — ordered content items with graphic type dropdown, duration field (number/hold/auto), background toggle, skip-if-no-data toggle, graphic params per type, advance condition selector — COMPLETE
3. Wire into RundownEditorPage.jsx — add playout and content-sequence to SEGMENT_TYPES array with colors, conditional editor panels in segment detail, type change preserves data — COMPLETE
