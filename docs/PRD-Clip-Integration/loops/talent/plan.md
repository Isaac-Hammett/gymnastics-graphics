# Talent — Tasks

1. Create TalentClipInfo.jsx — NOW SHOWING panel with athlete info, score (always visible), talking points (placeholder data), up next 2-3 items, per-mode display — COMPLETE
2. Wire TalentClipInfo into TalentView.jsx — conditional render when playout active, responsive breakpoints (mobile/tablet/desktop) — COMPLETE
3. Add Flag Moment button — simple one-click (no dialog), disabled when not in CLIP mode, signal to coordinator — NOT STARTED
